{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "ost-tools://_ost_tools_base",
  "description": "Shared definitions for OST tools schemas",
  "$defs": {
    "title": {
      "type": "string",
      "description": "Title of the node"
    },
    "content": {
      "type": "string",
      "description": "Body content"
    },
    "summary": {
      "type": "string",
      "description": "Short summary"
    },
    "status": {
      "type": "string",
      "enum": ["identified", "wondering", "exploring", "active", "paused", "completed", "archived"]
    },
    "priority": {
      "type": "string",
      "enum": ["p1", "p2", "p3", "p4"]
    },
    "assessment": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Assessment score from 1-5"
    },
    "wikilink": {
      "type": "string",
      "pattern": "^\\[\\[.+\\]\\]$",
      "description": "An Obsidian wikilink, e.g. [[Parent Node]]"
    },
    "baseNodeProps": {
      "type": "object",
      "properties": {
        "title": { "$ref": "#/$defs/title" },
        "content": { "$ref": "#/$defs/content" },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Categorization tags"
        }
      },
      "required": ["title"]
    },
    "ostEntityProps": {
      "type": "object",
      "properties": {
        "status": { "$ref": "#/$defs/status" },
        "summary": { "$ref": "#/$defs/summary" },
        "status_tweet": {
          "type": "string",
          "description": "Succinct free-text status summary"
        }
      },
      "required": ["status"]
    },
    "parentNodeProps": {
      "type": "object",
      "properties": {
        "parent": {
          "$ref": "#/$defs/wikilink",
          "description": "Parent node"
        }
      }
    }
  }
}
